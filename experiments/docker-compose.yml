version: '3.9'

services:
  prometheus-db:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
#      - ./path/to/datamount:/prometheus
